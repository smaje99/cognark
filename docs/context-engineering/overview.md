# Context Engineering Overview

Cognark's agentic layer should help agents work with a large project without reading everything on every task.

The system separates:

- canonical artifacts;
- derived indexes;
- summarized context;
- prompt packs;
- question memory.

The context layer should reduce token use while preserving traceability. Summaries are navigation aids, not source of truth.
