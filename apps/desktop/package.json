{
  "name": "@cognark/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "biome lint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
